name: 'projeto_supabase'
version: '1.0.0'
config-version: 2


profile: 'levi549'


model-paths: ["dbt/raw", "dbt/silver_gold"]

seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"


models:
  projeto_supabase:
    +materialized: table